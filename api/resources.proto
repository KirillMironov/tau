syntax = "proto3";
package api;
option go_package = "./;api";

service Resources {
  rpc Create(Request) returns (Response) {}
  rpc Remove(Request) returns (Response) {}
}

enum Kind {
  POD = 0;
}

message Request {
  Kind kind = 1;
  bytes data = 2;
}

message Response {}
